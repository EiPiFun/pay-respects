Description: <short summary of the patch>
 TODO: Put a short summary on the line above and replace this paragraph
 with a longer explanation of this change. Complete the meta-information
 with other relevant fields (see below for details). To make it easier, the
 information below has been extracted from the changelog. Adjust it or drop
 it.
 .
 pay-respects (0.6.10-1) unstable; urgency=medium
 .
   * Initial debian release (Closes: #1089677)
Author: Don Xu <me@eipi.fun>
Bug-Debian: https://bugs.debian.org/1089677

---
The information above should follow the Patch Tagging Guidelines, please
checkout https://dep.debian.net/deps/dep3/ to learn about the format. Here
are templates for supplementary fields that you might want to add:

Origin: (upstream|backport|vendor|other), (<patch-url>|commit:<commit-id>)
Bug: <upstream-bugtracker-url>
Bug-Debian: https://bugs.debian.org/<bugnumber>
Bug-Ubuntu: https://launchpad.net/bugs/<bugnumber>
Forwarded: (no|not-needed|<patch-forwarded-url>)
Applied-Upstream: <version>, (<commit-url>|commit:<commid-id>)
Reviewed-By: <name and email of someone who approved/reviewed the patch>
Last-Update: 2025-01-10

--- pay-respects-0.6.10.orig/core/Cargo.toml
+++ pay-respects-0.6.10/core/Cargo.toml
@@ -16,7 +16,8 @@ include = ["**/*.rs", "**/*.toml"]
 [dependencies]
 colored = "2"
 sys-locale = "0.3"
-rust-i18n = "3"
+#rust-i18n = "3"
+serde_yml = "0"
 regex-lite = "0.1"
 
 inquire = "0.7.5"
--- pay-respects-0.6.10.orig/module-request-ai/Cargo.toml
+++ pay-respects-0.6.10/module-request-ai/Cargo.toml
@@ -12,7 +12,8 @@ include = ["**/*.rs"]
 [dependencies]
 colored = "2"
 sys-locale = "0.3"
-rust-i18n = "3"
+#rust-i18n = "3"
+serde_yml = "0"
 serde_json = { version = "1.0" }
 serde = { version = "1.0", features = ["derive"]}
 textwrap = "0.16"
--- pay-respects-0.6.10.orig/core/src/main.rs
+++ pay-respects-0.6.10/core/src/main.rs
@@ -26,8 +26,8 @@ mod suggestions;
 mod system;
 
 #[macro_use]
-extern crate rust_i18n;
-i18n!("i18n", fallback = "en", minify_key = true);
+mod locale;use locale::rust_i18n;
+//i18n!("i18n", fallback = "en", minify_key = true);
 
 fn main() -> Result<(), std::io::Error> {
 	colored::control::set_override(true);
--- pay-respects-0.6.10.orig/module-request-ai/src/main.rs
+++ pay-respects-0.6.10/module-request-ai/src/main.rs
@@ -20,8 +20,8 @@ use textwrap::fill;
 mod requests;
 
 #[macro_use]
-extern crate rust_i18n;
-i18n!("i18n", fallback = "en", minify_key = true);
+mod locale;
+//i18n!("i18n", fallback = "en", minify_key = true);
 
 fn main() -> Result<(), std::io::Error> {
 	let command = std::env::var("_PR_LAST_COMMAND").expect("_PR_LAST_COMMAND not set");
--- pay-respects-0.6.10.orig/core/src/args.rs
+++ pay-respects-0.6.10/core/src/args.rs
@@ -1,3 +1,4 @@
+use crate::t;
 use crate::shell::{initialization, Init};
 use colored::Colorize;
 
--- pay-respects-0.6.10.orig/core/src/modes.rs
+++ pay-respects-0.6.10/core/src/modes.rs
@@ -1,3 +1,4 @@
+use crate::t;
 use crate::shell::Data;
 use crate::suggestions::suggest_candidates;
 use crate::system;
--- pay-respects-0.6.10.orig/core/src/shell.rs
+++ pay-respects-0.6.10/core/src/shell.rs
@@ -1,3 +1,4 @@
+use crate::t;
 use pay_respects_utils::evals::split_command;
 use pay_respects_utils::files::get_path_files;
 use std::process::{exit, Stdio};
--- pay-respects-0.6.10.orig/core/src/suggestions.rs
+++ pay-respects-0.6.10/core/src/suggestions.rs
@@ -1,3 +1,4 @@
+use crate::t;
 use std::io::stderr;
 use std::process::{exit, Stdio};
 use std::thread;
